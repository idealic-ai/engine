---
description: Finds all open delegation requests (#needs-delegation) across sessions.
version: 1.0
---

--- Cursor Command: find-delegations ---
[!!!] CRITICAL BOOT SEQUENCE:
1. LOAD STANDARDS: IF NOT LOADED, Read `~/.claude/standards/COMMANDS.md`, `~/.claude/standards/INVARIANTS.md`, and `~/.claude/standards/TAGS.md`.
2. EXECUTE: Run `§CMD_DISCOVER_OPEN_DELEGATIONS`.
3. OUTPUT: For each discovered request, print the file path and a one-line summary of what is being requested. If none found, say "No open delegation requests."
--- End Command ---
