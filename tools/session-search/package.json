{
  "name": "session-search",
  "version": "1.1.0",
  "description": "Semantic search over session history using Gemini embeddings + sql.js (WASM SQLite)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/genai": "^0.7.0",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
