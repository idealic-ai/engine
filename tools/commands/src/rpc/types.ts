/**
 * commands namespace types.
 *
 * Centralized type maps (CommandsCommands, CommandsNamespace, etc.) have been
 * replaced by per-handler module augmentation of Registered in each handler file.
 */
