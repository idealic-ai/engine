# Session Report — [state line]
**Tags**: #report
**Generated**: [YYYY-MM-DD HH:MM] · **Session**: `[<slug>]`

> **State strip**: branch `[name]` · work-repo `[committed | N uncommitted]` · [N] dirty · [plan x/y — omit if no plan] · [N] open threads

## Where we stand
*The one-paragraph orientation. What is true right now.*

[State line expanded into 2–4 sentences: what this work is, what phase/step it's at, what just happened, what it's waiting on.]

## Reconciliation deltas
*The payload: where the transcript corrected the live bearings — and any stale narrative artifact (a debrief / ticket / PR body written before the last real work). This is the highest-value section; keep it near the top. Omit only if genuinely none.*

*   Believed [X]; transcript shows [Y].
*   Stale: `[artifact]` predates [the last real work] — [what's now out of date].

## Work done
*Substantive accomplishments, most recent first — grounded in transcript + git.*

*   [Thing 1]
*   [Thing 2]
*   [Thing 3]

## Git
*Per repo the work actually touched — label which holds this session's work.*

*   **`[repo]`** — branch `[name]`, [N dirty | clean], last commit `[sha] [subject]` — [holds this session's work: committed / uncommitted]
*   **`[other repo]`** — [state] — [unrelated / secondary]

## Ticket
*   **[KEY]** — [status] — [most relevant open point, or "branch-derived, unrelated to this work", or "none linked"]

## Trajectory
*The /graph flowgraph — rendered only when the trajectory actually branches. If linear, this section is replaced by the ordered "What's next" list below.*

```
[flowgraph, or "— linear; see What's next —"]
```

## Pending on you
*Decisions / questions explicitly awaiting the human. Mined especially from AskUserQuestion exchanges left unresolved.*

*   [Decision 1] — [context]
*   [Decision 2] — [context]

## What's next
*Immediate next actions, ordered.*

1.  [Next action]
2.  [Then this]

## Open threads / dangling
*Questions raised and never closed, `#needs-*` tags, deferred items, stale artifacts, "we'll come back to X" promises. Each with a disposition.*

*   [Thread] — [disposition: address / deferred / dismissed]
