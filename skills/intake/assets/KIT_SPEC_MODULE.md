# Retired

`KIT_SPEC_MODULE.md` is retired, **superseded by `kit-library/docs/extras/KIT_SPEC_IDEA.md`** — whose numbered rules are also carried, one verbatim line each, in `kit-library/docs/extras/RULES.md`.

It already carried a RETIRED banner on line 3 — and kept 424 lines of body under it, so a grep landed
mid-document and never saw the banner. **That is why the body is cut here rather than banner-flagged
again**: a retirement a search cannot see is not a retirement.

Its identity vocabulary is extinct, not deprecated: `data-mod` occurs **0** times in `idea-cli/`,
against 91 for the live `idea-def`. Two CLI comments still cite it as a source for one argument —
`lib/clause-dom.ts:5` and `lib/clause-eval.ts:10`, both naming `KIT_SPEC_MODULE:59` for the "two
pointers at one fact" defect. Those citations resolve into the body below, by line number.

Body, verbatim: **`_attic/D13_RETIRED_KIT_SPEC_MODULE.md.txt`**.
