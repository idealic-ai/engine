# Retired

`KIT_SPEC_PUBLISHING.md` is retired. It specifies the **predecessor warm-print kit** — it names
`kit-behaviors.js` and `proof-ticket`, neither of which the live tool touches (`kit-behaviors` has 0
hits in `idea-cli/`). The live surfaces are `idea-cli/verbs/publish.ts` and `idea publish --help`.

**Not a claim that the predecessor kit is dead.** It is a second live design system, owned by
`/prove` and `/remix`, and `KIT_README.md` — which stays in this directory — is authoritative for it.
This document is retired as a *specification of the idea library*, which it never was.

Body, verbatim: **`_attic/D14_RETIRED_KIT_SPEC_PUBLISHING.md.txt`**.
