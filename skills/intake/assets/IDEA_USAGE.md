# Retired

`IDEA_USAGE.md` is retired. Composing a page — where composition starts, the one script, the relations
that wire it, and where depth comes from — is **`PAGE.md`**, which also carries
`¶INV_A_PAGE_IS_AN_IDEA`, `¶INV_A_CLASS_IS_NOT_AN_IDEA`, `¶INV_AN_ARRANGEMENT_NEEDS_A_CLAUSE` and
`¶INV_DEPTH_IS_DERIVED_NEVER_DECLARED`.

`¶INV_WHAT_AN_IDEA_EXPECTS_IS_NOT_WHAT_IT_BRINGS` moved to `IDEA.md`, beside the `host contract` row
it governs.

Read `PAGE.md`.
