# Retired

`METADATA.md` is retired. It straddled two concerns and each went to its own document.

- **The three layers** (metadata · relations · rules) and the test that assigns a fact to one →
  **`LIBRARY.md`**
- **The predicate language** — fitness-is-a-predicate, the same grammar as rules, the five targets,
  the worked predicates, the host predicate, an-unknown-is-not-a-fit → **`QUERY.md`**
- **The fields** — `kind`, `description`, `fits`, `not for`, and what is never a field → **`IDEA.md`**

Read `QUERY.md` for the grammar, `IDEA.md` for the rows.
