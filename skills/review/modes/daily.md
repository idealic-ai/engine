# Daily Mode — SUPERSEDED

This mode has been replaced by **Narrative mode** (`modes/narrative.md`), which is a strict superset: waterline-driven self-defining window, timezone-honest boundaries, author git log + Linear ticket digest + filtered continuation logs in the bundle, running narrative continuity (banked "shifts in thinking" + headline arc), volume-scaled sizing with a two-pass Gemini expansion for longer reports, and a real `gemini` CLI invocation.

The old Daily mode referenced an `engine gemini` command that does not exist — use the `gemini` CLI directly (see `modes/narrative.md` Phase 3, which sources `GEMINI_API_KEY` from the repo-root `.env`).

Not wired into the SKILL.md `modes` object. Kept only as a redirect breadcrumb.
